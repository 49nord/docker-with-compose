**Docker 19.03.8** image that contains `docker-compose`, `curl`, `git`, `git-lfs` and `openssh-client`.

**Docker 19.03.8** is what's used in GitLab CI documentation at the time of writing (2020-05-29).
